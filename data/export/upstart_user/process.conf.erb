start on starting <%= app %>-<%= name %>
stop on stopping <%= app %>-<%= name %>
respawn

chdir <%= engine.root %>
env PORT=<%= port %>

script
  . $HOME/.bashrc
  <%= process.command %> >> <%= log %>/<%=name%>-<%= app %>.log 2>&1
end script

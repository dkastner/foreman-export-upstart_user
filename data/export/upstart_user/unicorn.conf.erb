start on starting <%= app %>
stop on stopping <%= app %>
respawn

chdir <%= engine.root %>
env WEB_CONCURRENCY=<%= num %>

exec sudo -i -u inspire bash -c "bin/unicorn -c unicorn.rb >> <%= log %>/<%=name%>.log 2>&1"
